import React from "react";
import { Header } from "../../";
const LoginPage = ({ className }) => {
  return (
    <div className={`${className} LoginPage`}>
      <Header title="Login" />
    </div>
  );
};

export default LoginPage;
